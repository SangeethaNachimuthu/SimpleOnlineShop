package lexicon;

/*
OrderStatus is an enum class, which defines the possible states of an order
such as created, confirmed, shipped and canceled.
 */
public enum OrderStatus {

    CREATED,
    CONFIRMED,
    SHIPPED,
    CANCELED
}
